{
  "id": "onchain-router",
  "name": "Onchain Router",
  "description": "Use policy-bounded, receipt-backed LLM calls through the local Onchain Router Buyer Runtime.",
  "version": "0.2.1",
  "activation": { "onStartup": true },
  "enabledByDefault": false,
  "providers": ["onchain-router"],
  "contracts": {
    "tools": [
      "onchain_router_models",
      "onchain_router_pricing",
      "onchain_router_voices",
      "onchain_router_image_generate",
      "onchain_router_speech_generate",
      "onchain_router_transcribe"
    ]
  },
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "proxyOrigin": {
        "type": "string",
        "default": "http://127.0.0.1:8402",
        "description": "Loopback origin of the separately running Onchain Router buyer proxy."
      },
      "tokenFile": {
        "type": "string",
        "description": "Owner-only file containing the buyer proxy bearer. Defaults to ~/.onchain-router/proxy-token."
      },
      "profileDirectory": {
        "type": "string",
        "description": "Buyer profile directory. Defaults to the directory containing tokenFile."
      },
      "manageProxy": {
        "type": "boolean",
        "default": true,
        "description": "Start the exact installed loopback proxy when it is not already reachable."
      }
    }
  }
}
